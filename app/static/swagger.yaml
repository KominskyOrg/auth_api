openapi: 3.0.0
info:
  title: Stack API
  version: 1.0.0
  description: xxxDESCRIPTIONxxx
paths:
  /api/stack/health:
    get:
      summary: Health check for Stack API
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: OK
